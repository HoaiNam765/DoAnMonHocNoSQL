# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase service account key — KHÔNG commit lên git
firebase-service-account.json
backend/firebase-service-account.json
# Cấu hình môi trường — KHÔNG commit lên git.
# Cả backend lẫn frontend nay dùng chung một file .env ở thư mục gốc.
.env
*.env
.env.local
.env.*.local

# Nhưng file MẪU thì PHẢI commit để người trong nhóm biết cần điền những gì.
# Nó không chứa giá trị thật, chỉ có tên biến và ghi chú.
!.env.example
